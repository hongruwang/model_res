	  
	  COMMON /ECM1/ ADVUA(IJM),ADVVA(IJM),ADVUU(IJM),ADVVV(IJM)
      COMMON /ECM2/ DRHOX(IJM,KB),DRHOY(IJM,KB),TRNU(IJM)
      COMMON /ECM3/ TRNV(IJM), TBDRY2(EBCM,KBM1), SBDRY2(EBCM,KBM1)
      COMMON /ECM4/ DUMP1(IJM),DUMP2(IJM,KB)
	  COMMON /ECM5/ DUMB1(QBCM),DUMB2(EBCM,KBM1),DUMB3(DBCM)
	  COMMON /ECM6/ COHTHK(IJM),NCOHTHK(IJM),timebegin,timeend
	  COMMON /ECM7/ myid,numprocs,rc 
      REAL DUMB1,DUMB2,DUMB3
	  REAL COHTHK,NCOHTHK


CW    ================定义双精度=================

CW 	  DOUBLE PRECISION ADVUA,ADVVA,ADVUU,ADVVV,timebegin,timeend

 
 
      INTEGER   myid,numprocs,i,rc 
 

	 EQUIVALENCE (IVAR,TPS),(PRT,A)
